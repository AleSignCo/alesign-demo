{
  "name": "alesign-demo",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "AleSign: site demo per lead (/<slug>), Mostra (/m/<slug>), contract (/c/<slug>), onboarding (/o/<slug>)",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "@pdf-lib/fontkit": "^1.1.1"
  }
}
